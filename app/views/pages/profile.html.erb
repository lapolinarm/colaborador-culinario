<Section class="container">
  <div class="text-center my-4">
    <h1>Perfil de usuario</h1>
  </div>

  <div class=" container mb-4 d-flex flex-column align-items-center gap-4">
    <div class="row align-items-center">

        <div class="my-4 text-center">
          <h1 class="custom-title" id="text-custom">Bienvenid@ <%= current_user.first_name + " " + current_user.last_name%></h1>
        </div>

    </div>
    <div class="row">

      <div class="col-md-6 my-4 d-flex align-items-stretch ">
        <img class="avatar-img mx-auto levitate" src="https://res.cloudinary.com/roldan-cloudinary/image/upload/v1726246081/colaborador%20culinario/ktscqsdwihjo4fgfo957.jpg" alt="profile-pic">
      </div>

      <div class="col-md-4 my-4 d-flex align-items-stretch">
        <h5 class="custom-cards align-self-center"><strong>Rol: </strong><span id="text-custom"><% if current_user.role  == "collaborator" %>
            colaborador <i class="fa-solid fa-utensils"></i>
          <% else current_user.role  == "owner" %>
            empresario <i class="fa-solid fa-briefcase"></i>
          <% end %>
        </span>
        </h5>
      </div>

      <div class="container-fluid row custom-container my-5" >
        <div class="col-md-4 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Nombre: </strong><span class="font-size-custom"><%= current_user.first_name.present? ? current_user.first_name : "No hay un nombre disponible." %></span></h5>
          </div>
        </div>

        <div class="col-md-4 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Apellido: </strong><span class="font-size-custom"><%= current_user.last_name.present? ? current_user.last_name : "No hay un apellido disponible."  %></span></h5>
          </div>
        </div>

        <div class="col-md-4 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>DNI: </strong><span class="font-size-custom"><%= current_user.dni.present? ? current_user.dni : "No hay un DNI disponible."  %></span></h5>
          </div>
        </div>



        <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Teléfono: </strong><span class="font-size-custom"><%= current_user.mobile_phone.present? ? current_user.mobile_phone : "No hay número de teléfono disponible." %></span></h5>
          </div>
        </div>

        <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Correo: </strong><span class="font-size-custom"><%= current_user.email.present? ? current_user.email : "No hay un correo disponible." %></span></h5>
          </div>
        </div>

        <div class="col-md-12 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Dirección: </strong><span class="font-size-custom"><%= current_user.address.present? ? current_user.address : "No hay una dirección disponible."  %></span></h5>
          </div>
        </div>
      </div>

      <div class="container-fluid row custom-container my-5">
        <% if current_user.payment_method == 1 %>
        <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Método de pago: </strong><span class="font-size-custom"> Por transferencia</span></h5>
          </div>
        </div>

        <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Mi banco: </strong><span class="font-size-custom"><%= current_user.bank.present? ? current_user.bank : "No hay un banco disponible." %></span></h5>
          </div>
        </div>

        <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Nombre del titular de la cuenta: </strong><span class="font-size-custom"><%= current_user.name_holder.present? ? current_user.name_holder : "No hay un titular de la cuenta disponible." %></span></h5>
          </div>
        </div>

        <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
          <div class="mb-4 text-center">
            <h5 class="custom-cards"><strong>Mi número de cuenta: </strong><span class="font-size-custom"><%= current_user.bank_account.present? ? current_user.bank_account : "No hay un número de cuenta disponible." %></span></h5>
          </div>
        </div>
      </div>
      <% elsif  current_user.payment_method == 2 %>

      <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
        <div class="mb-4 text-center">
          <h5 class="custom-cards"><strong>Método de pago: </strong><span class="font-size-custom"> billetera digital</span></h5>
        </div>
      </div>

      <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
        <div class="mb-4 text-center">
          <h5 class="custom-cards"><strong>Mi número de cuenta: </strong><span class="font-size-custom"><%= current_user.digital_wallet.present? ? current_user.digital_wallet : "No hay un número de cuenta disponible." %></span></h5>
        </div>
      </div>

      <% else %>

      <div class="col-md-6 my-4 d-flex align-items-stretch justify-content-center">
        <div class="mb-4 text-center">
          <h5 class="custom-cards"><strong>Método de pago: </strong><span class="font-size-custom"> Efectivo</span></h5>
        </div>
      </div>
      <% end %>
    </div>
  </div>

  <style>
    .font-size-custom {
      font-size: 18px;
      color: #6c757d;
    }

    #text-custom {
      color: #6c757d;
    }
    .custom-title {
        background-color: #DAD0FE;
        border-radius: 8px;
        padding: 1.5rem;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        color: black;
      }

    .custom-cards {
      background-color: #F4F4F4;
      border-radius: 8px;
      padding: 1.5rem;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      color: black;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .custom-cards:hover {
      background-color: #00AE7A;
      color: white;
      transform: translateY(-10px);
      box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1);
    }

    .avatar-img {
      border-radius: 50%;
      width: 200px;
      height: 200px;
    }

    @keyframes levitate {
      0% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
      100% {
        transform: translateY(0);
      }
    }


    .levitate {
      animation: levitate 3s ease-in-out infinite;
      transition: box-shadow 0.3s ease;
    }

    .levitate:hover {
      box-shadow: 0 10px 15px rgba(0, 0, 0, 0.1); /* Opcional: para un efecto de sombra en hover */
    }

    .custom-container {
      border-radius: 15px;
      padding: 1.5rem;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      color: black;
    }
  </style>

</Section>
